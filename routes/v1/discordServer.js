import express from 'express';

import { createDiscordServerController } from '../../controllers/discordServerController.js';

const router = express.Router();

// router.post('/create-server',isAuthenticate,validate(), createDiscordServerController)


export default router;